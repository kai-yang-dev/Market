## Admin environment variables (Vite)
# Create a file named `.env` in this same `admin/` folder and copy these values in.
#
# Backend API base URL (should include `/api`)
VITE_API_URL=http://localhost:3000/api
#
# Backend base URL (no `/api`) - used for images/uploads, etc.
VITE_BACKEND_URL=http://localhost:3000
#
# Socket.IO base URL (no `/chat`)
VITE_SOCKET_URL=http://localhost:3000
#
# Dev proxy target used by `vite.config.ts` (no path). Optional.
VITE_PROXY_TARGET=http://localhost:3000


